version: '3.8'

services:
  sqtpm-web:
    build: .
    ports:
      - "8080:80"
    volumes:
      - ./data:/var/www/data  # For storing uploaded files if needed
    environment:
      - APACHE_RUN_USER=www-data
      - APACHE_RUN_GROUP=www-data
    restart: unless-stopped
